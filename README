Attempt to turn https://github.com/mgottschlag/linux 's Focaltech driver into a
DKMS Driver for Ubuntu 14.04.

Currently not working.


Installation instructions
=========================

sudo apt-get install dkms

cd /usr/src/
sudo git clone https://github.com/markmandel/psmouse-focaltech-dkms.git
sudo dkms add -m psmouse -v focaltech-dkms
sudo dkms build -m psmouse -v focaltech-dkms
sudo dkms install -m psmouse -v focaltech-dkms

Uninstallation
=================================
sudo dkms remove psmouse/focaltech-dkms --all
